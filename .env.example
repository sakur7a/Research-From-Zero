# Optional. Keep this file free of real credentials.
# Environment variables must be exported by your shell or passed to Docker.
# RE0_DB=.data/re0.sqlite3
# GITHUB_TOKEN=  # Optional public-read GitHub token; never sent to the browser.
# RE0_HOST=127.0.0.1
# RE0_PORT=8000
