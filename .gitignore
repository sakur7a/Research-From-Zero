.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
.coverage
htmlcov/
.env
.env.*
!.env.example
.data/
.learnbuddy/
*.sqlite3
*.sqlite3-*
*.db
node_modules/
test-results/
playwright-report/
.DS_Store
backups/
# Regenerable evaluation run records; evals/results/SUMMARY.md is the record.
evals/runs/
# Left behind by an interrupted atomic write, and the control probe that proved deletion is refused
# in this environment. Neither can be removed here (the recycle bin is unavailable, so the safety
# shim fails closed), so they are ignored to guarantee they can never be committed. The list in
# to-delete/README.md is where they are tracked for manual removal.
backend/tests/test_mcp.staged
backend/tests/delete-probe.txt
*.egg-info/
build/
dist/
/re0-library.json
/re0-library.bib
